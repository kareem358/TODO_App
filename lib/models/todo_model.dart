class ToDoModel{//creating a class of todo model which will
  // will be store in a list form.
  String title;
  String desc;
  bool check =false;
  ToDoModel(this.title,
      this.desc,
      this.check);
}